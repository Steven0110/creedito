{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Send Reset Email Schema",
	"description": "Modelo de Envio de email de recuperación de cuenta",
	"type": "object",
	"properties": {
		"username": {
			"type": "string",
			"pattern": "^[A-z0-9 .]+@[A-z0-9 .]+$"
		}
	},
	"required": ["username"],
	"additionalProperties": false
}